{
	"$schema": "http://json-schema.org/draft-07/schema",
	"title": "app config schema",
	"type": "object",
	"required": [
		"BACKEND_URL",
		"PORT",
		"DEMO_MODE",
		"VERSION"
	],
	"properties": {
		"BACKEND_URL": {
			"type": "string",
			"description": "the protocol and url with port of the backend server",
			"format": "uri"
		},
		"PORT": {
			"type": "integer",
			"description": "the binding port of this server",
			"default": 3000,
			"minimum": 0,
			"maximum": 9999
		},
		"DEMO_MODE": {
			"type": "boolean",
			"default": false,
			"description": "this enables demo mode of the app"
		},
		"VERSION": {
			"type": "string",
			"description": "a version number with pattern validation",
			"pattern": "^(([0-9]+.){2})([0-9]+)$"
		}
	}
}
