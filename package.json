{
  "homepage": "",
  "jest": {},
  "scripts": {
    "test": "tsc && tslint --project tsconfig.json && jest --verbose --coverage",
    "watch:test": "jest --watch"
  },
  "devDependencies": {
    "@types/jest": "^24.0.20",
    "coveralls": "^3.0.7",
    "jest": "^24.9.0",
    "tslint": "^5.20.0",
    "typescript": "^3.6.4"
  }
}
